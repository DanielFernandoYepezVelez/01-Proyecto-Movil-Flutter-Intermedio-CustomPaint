import 'package:flutter/material.dart';

import 'package:disenos_app/src/widgets/headers.dart';

class HeadersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // body: HeaderCuadrado(),
      // body: HeaderBordeRedondeado(),
      // body: HeaderDiagonal(),
      body: HeaderDiagonal(),
    );
  }
}
